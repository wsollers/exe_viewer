// Scaffold for your GUI layer.
// Next iteration: GLFW + Vulkan swapchain + Dear ImGui backends; then display parsed data.

#include <cstdint>

namespace peelf::viewer {

void placeholder() {
    volatile std::uint32_t v = 0;
    (void)v;
}

} // namespace peelf::viewer
