# PE / ELF Explorer — Development Plan & ToDo

A roadmap to take `peelf_explorer` from an initial scaffold to a full cross-format
binary viewer (PE/ELF, exe/dll/so) with header/section/import-export inspection,
raw-byte view, disassembly, and decompilation — while paying down existing tech debt
and raising overall code quality.

> Legend: `[ ]` not started · `[~]` in progress · `[x]` done
> Task IDs (`P1-3`, `TD-2`, …) are referenced across sections so work can be tracked and cross-linked.
>
> **Progress (Phase 1):** Phase 0 complete (P0-1 … P0-10). Phase 1 in progress — P1-1 / P1-2 done; P1-3 / P1-4 implemented in the **core** (`PeImage` / `ElfImage` identity behind `IBinaryImage`, via `parse_image`), with the app-side migration (BinaryModel/panels onto `IBinaryImage`, removing the legacy parsers) still pending. Unit suite: 52 tests. The app load path now calls `parse_image()` (ELF files load; PE panels still on the legacy `PeModel`); full panel migration + legacy-parser removal pending. Phase 2 started: section tables parsed for PE/ELF (P2-1), ELF program-header segments parsed and shown in the Sections panel, and `IBinaryImage` now exposes checked file-offset ↔ virtual-address mapping. Phase 3 started: ELF `.symtab` / `.dynsym` symbols parse into shared `Symbol` values and display in a Symbols panel; ELF `DT_NEEDED` libraries and PE import directories parse into shared `ImportEntry` values and display in a unified Imports panel; PE export directories parse into shared `ExportEntry` values and display in a unified Exports panel. Deterministic parser fixtures now cover PE32/PE32+, PE base relocations, PE debug directory / CodeView records, PE runtime-function / exception directory entries, PE delay-load imports, PE bound imports, PE resource directories/data entries, PE CLR headers, PE TLS, PE certificate tables, PE Load Config / Guard CF fields, ELF32/ELF64, little-/big-endian ELF identity, ARM32/ARM64, RISC-V32/RISC-V64, MIPS32/MIPS64, PowerPC32/PowerPC64, `.text` mapping, raw ELF `.symtab` records, raw ELF `.dynamic` records, raw ELF `.rel.dyn` / `.rela.dyn` relocation records, `PT_INTERP` interpreter paths, ELF notes from both `PT_NOTE` and `SHT_NOTE`, and SysV/GNU ELF hash tables. ELF file-header, program-header, section-header, symbol-table, dynamic-entry, relocation, interpreter, note, and hash-table parsing now exposes every raw field or resolved value currently modeled and is tested across the fixture matrix; PE base-relocation blocks, CodeView debug records, runtime functions, delay-load imports, bound imports, resource directories/data entries, CLR headers, TLS directories, certificate tables, and Load Config directories are now exposed and tested for PE32/PE32+. Disassembler is now initialized from the unified image (PE **and** supported ELF architectures), ELF entry-point disassembly resolves through core VA→file-offset mapping, clicking a byte in the Hex view disassembles a window using the mapped VA when available (P4-1 partial), unsupported architectures no longer fall back to bogus x64 disassembly, and Capstone mode setup is covered by tests for x86-64, ARM64, MIPS32/MIPS64, PowerPC32/PowerPC64, RISC-V32, and RISC-V64; also fixed an ELF crash in `on_file_loaded` (unconditional null-`pe()` deref, exposed once ELF files began loading). The first UI navigation-model slice is in place: a tested, ImGui-independent structure tree builds selectable nodes from `IBinaryImage` for PE and ELF. The viewer now starts Hex at the mapped entry point, supports byte-range navigation/highlighting, and wires section/symbol/import/export/structure selections into Details plus Hex/Disassembly where the target maps to file bytes. PE structure navigation now includes runtime functions, TLS, certificates, load config, delay-load imports, bound imports, resource directory/data, and CLR header nodes.

---

## 1. Goals

1. **One model, many formats.** Parse PE and ELF uniformly behind a common interface, exposing shared concepts (sections, symbols, imports/exports, entry point, architecture) plus format-specific detail. Correctly distinguish executables vs. shared libraries (exe/dll vs. so).
2. **Five core views**, each format-aware:
   - Headers / summary (PE COFF + optional header; ELF header + program headers)
   - Sections (and ELF segments)
   - Imports / Exports (PE import/export tables; ELF dynamic symbols + needed libraries)
   - Raw bytes (hex/ASCII, with navigation to file offsets / RVAs)
   - Disassembly (Capstone) and **Decompilation** (pseudo-C)
3. **Live target inspection.** Open static files or attach read-only to a running process, starting with Win64 PE modules and Linux x64 ELF modules. Show process/module/thread/handle-style inventory, VMMap-style memory regions, live-memory hex/disassembly, and clear permission/staleness failures.
4. **Security posture reporting.** Surface BinSkim-style executable hardening facts: mitigation flags, debug/repro metadata, signing/certificate state, loader protections, section permissions, and suspicious configuration. Keep this as factual reporting first; policy scoring can come later.
5. **Production-quality code.** Consistent style, real error handling, no dead/duplicate paths, tests, and CI.

> **Testing is mandatory and gating.** Every feature — current and new — must have unit tests.
> A change (feature, refactor, or tech-debt fix) is **not complete** until the full unit-test
> suite builds and passes (`ctest` green). GoogleTest is the framework; tests live in `tests/`.
> No exceptions: code without passing tests is treated as unfinished.

## 1a. Current Feature Groups

- [x] **FG-PE-EDGE** — PE import/export edge cases. Implement and test ordinal imports, forwarder exports, and malformed-table negative cases. Cross-links: P3-1, P3-2, P3-6, P7-7. **Completed:** ordinal-import parsing and display, forwarder-export fixture coverage, and malformed import/export RVA negative tests are implemented and pass on Windows/MSVC and Linux/Clang-in-Docker.
- [x] **FG-GRAPH-CLICK** — Call graph click-through hardening. Graph node clicks now use a UI-independent selection payload, update Details/Hex/Disassembly, synchronize and scroll Symbols when a symbol row is known, and visually highlight the selected graph node. Cross-links: P4-5, P7-10, P7-12b.
- [~] **FG-CFG** — Function/basic-block CFG construction. CFG model now builds basic-block nodes, conditional/unconditional branch edges, fallthrough edges, return edges, and graph byte ranges from x86-64 byte sequences, the `first` function across the ELF bin-matrix architecture fixtures, and the PE x86/x64 DLL fixtures. Remaining: richer architecture-specific branch classification, call/branch edge detail from disassembly metadata, and CFG mode in the call graph UI. Cross-links: P4-5, P7-9, P7-12b.
- [ ] **FG-PE-UNWIND** — PE unwind / `.pdata` / `.xdata` detail. Parse and display runtime-function ranges, unwind versions/flags, prolog size, unwind codes, chained info, and exception/termination handlers. Cross-links: P3-6, P4-2, P7-7.
- [ ] **FG-DEPS** — Dependency tree panel. Static mode uses PE imports/delay-load/bound imports and ELF `DT_NEEDED`; live mode reuses the same model for loaded process modules. Cross-links: P6-20a, P7-12a.
- [ ] **FG-SECURITY** — Security hardening report. PE first: ASLR, NX, CFG, high-entropy VA, relocations, certs, debug info, W+X sections; ELF later: PIE, NX, RELRO, RPATH/RUNPATH, W+X, stripped/debug info. Cross-links: P6-19, P7-16.
- [~] **FG-PATCHING** — Staged binary patching foundation. Implement a disk-backed `PagedBinaryImage`, normalized `PatchOverlay` intervals, transaction commit/rollback, undo/redo, dirty interval reporting, and fixed-size byte replacement tests across page boundaries and overlapping edits. First slice is in progress in `peelf_core`; future slices wire the shellcode/assembler panel into transactions, then expose the same API to Lua scripting. Cross-links: P2-3, P6-20b, P6-21, P6-22, P6-23, P6-24.
- [ ] **FG-SCRIPTING** — Binary modification scripting. Add a small scripting host over the patch transaction API, starting with Lua for low embedding/package cost; leave Python as a later optional provider after the host API is stable. Scripts must use transactions, support rollback on failure, and expose read/write/find/assemble/disassemble helpers through the same core API. Cross-links: FG-PATCHING, P6-24.

## 2. Key decisions & open questions

Resolve these before the phases they gate; defaults are the recommended path.

- **D1 — Decompiler backend (gates Phase 5b).** Options:
  - *RetDec* (LLVM-based, open source) invoked as a subprocess, output parsed and displayed. **Recommended default** — good quality, no Java, swappable.
  - *Ghidra headless* (`analyzeHeadless` + export script) — best quality, but heavyweight Java dependency.
  - *Binary Ninja* (Free / personal / non-commercial) optional backend — good x86/x64 workflow and exposed API, especially useful for personal tooling. First integration should use a Python subprocess bridge so the viewer still builds/runs without BN installed; direct C++ SDK integration is deferred until the workflow proves valuable.
  - *In-tree pseudocode-lite only* — function/basic-block reconstruction over Capstone; lowest fidelity, no extra deps.
  - **Decision:** wrap whatever we pick behind `IDecompiler` (Phase 5a) so the choice is reversible. _Owner to confirm D1 default._
- **D2 — GUI scope.** Stay with Dear ImGui + Vulkan (current stack) vs. simplify the renderer. **Default: keep ImGui**, but harden the Vulkan layer (TD-7, TD-13) and pin ImGui (TD-10).
- **D3 — Platform priority.** **Default: Windows (MSVC) is primary** (matches current dev box), Linux/Clang kept green in CI.
- **D4 — Min toolchains.** C++23 requires VS 2022 ≥ 17.6 / Clang 18 + libstdc++ 13. Confirm we can require these.
- **D5 — Live-process scope.** Start with local, read-only attach only: Windows x64 PE modules via Toolhelp/PSAPI/NtQuery*/ReadProcessMemory; Linux x64 ELF modules via `/proc`, `process_vm_readv`, and `/proc/<pid>/mem` where permitted. No write/patch/debug-control features until the read-only model is stable.
- **D6 — Security reporting stance.** Start with transparent facts and rule IDs inspired by BinSkim output, not pass/fail compliance claims. Each finding must cite parsed evidence (header flag, directory entry, section characteristic, certificate/debug record, or live mapping property).
- **D7 — Byte editing / patching stance.** Editing must be explicit and staged. Capstone is the disassembly engine only; binary generation from assembly should use an assembler backend such as AsmJit/AsmTK for supported arches and eventually LLVM MC for broader cross-assembly. Static-file edits first create a disk-backed paged image plus patch overlay and require an explicit save/export action. Live-process edits are out of scope until read-only live target support is stable and separately approved.

## 3. Target architecture

Proposed layout (incremental migration, not a big-bang rewrite):

```
libs/peelf_core/
  include/peelf/
    error.hpp          # Error + Result<T> = std::expected<T, Error>
    byte_reader.hpp    # fixed endian-aware reader (no broken vtable)
    binary_image.hpp   # IBinaryImage interface + shared value types
    pe/                # PeImage : IBinaryImage
    elf/               # ElfImage : IBinaryImage
    mapping/           # FileMapping (cleaned up)
  src/ …
apps/viewer/
  src/
    app/               # application, main, config
    render/            # vulkan_manager + imgui glue   (was vulkan/)
    panels/            # UI panels                      (was ui/)
    disasm/            # disassembler (spelling fixed)  (was dissassembler/)
    decompile/         # IDecompiler + backend adapter
    viewmodel/         # adapts core IBinaryImage -> panel-friendly data
```

**Shared value types** (in `binary_image.hpp`): `Architecture`, `Endianness`,
`ImageKind { Executable, SharedLibrary, Object, Core, Unknown }`, `Section`,
`Segment`, `Symbol`, `ImportEntry`, `ExportEntry`, `RelocationGroup`, `StringRef`.
`IBinaryImage` exposes these uniformly; PE/ELF-only extras are reachable via the
concrete type (downcast) or a `format_details()` `std::variant`.

## 4. Cross-cutting conventions

- [ ] **C1** — Error handling: `Result<T> = std::expected<T, peelf::Error>` everywhere in core; no silent failure, no exceptions across the core API boundary (exceptions allowed only at the app's `main`/Vulkan layer).
- [ ] **C2** — Naming/style: snake_case for funcs/locals, `PascalCase` types, trailing `_` for members; enforce via `.clang-format` (already present) + `.clang-tidy` (new).
- [ ] **C3** — Warnings are errors in CI for our own targets (keep third-party warnings suppressed). Current flags already include `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion` / `/W4 /permissive-`.
- [ ] **C4** — **Testing is mandatory and gating.** Every feature, fix, and refactor — current and new — ships with unit tests, and the complete `ctest` suite must build and pass before the change is considered complete. GoogleTest is the framework, wired in under `tests/` (gated by `PEELF_BUILD_TESTS`, discovered by CTest). Backfill tests for existing functionality as it is touched.
- [~] **C5** — Fixed-width primitive cleanup: binary-facing data and parser/disassembler paths should use `std::uint*_t` / `std::size_t` rather than implementation-sized aliases. First pass covered the unified parser, disassembler wrapper, key UI disassembly paths, smoke-test byte handling, and PE address-map overflow helpers. Remaining: legacy PE parser structs/helpers, Vulkan/GLFW-required callback integers, and text-facing `char` buffers that must stay compatible with C APIs.

---

## 5. Phase 0 — Tech-debt cleanup & foundations — COMPLETE

**Status: complete (P0-1 … P0-10)** — clean build + green test suite on MSVC. Each item maps to the
**Tech Debt Register** in §13.

- [x] **P0-1 (TD-1)** Doc/dependency reconciliation finished. Rewrote `third_party/README.md` to describe the FetchContent reality (GLFW 3.4, ImGui `docking`, NFD v1.2.1, Capstone 5.0.6; Vulkan from the installed SDK; no vendored dirs). Added a `LEGACY / UNSUPPORTED - DO NOT RUN` banner to `scripts/bootstrap_deps.ps1`, `scripts/bootstrap_deps.sh`, and `scripts/getdeps.ps1`, and added `scripts/README.md` documenting why they're legacy. (`README.md` was already updated earlier.) Docs-only — no build/test impact.
- [x] **P0-2 (TD-10)** Pinned Dear ImGui in `third_party/dependencies.cmake` to docking commit `2af6dd9694288e6befe1edb7ce25510911693c22` (2026-06-04) instead of the moving `docking` tip. Verified via GitHub that this commit's `backends/imgui_impl_vulkan.h` contains every `ImGui_ImplVulkan_InitInfo` field `application.cpp` sets (`PipelineInfoMain`/`PipelineInfoForViewports`, `ApiVersion`, `DescriptorPoolSize`, `MinAllocationSize`, `CustomShaderVert/FragCreateInfo`). Set `GIT_SHALLOW FALSE` (shallow clone can't check out an arbitrary SHA) and updated `third_party/README.md`. _Note:_ the top-level `FETCHCONTENT_UPDATES_DISCONNECTED ON` means a clean reconfigure (or clearing the imgui `_deps` entries) is needed for the new pin to take effect.
- [x] **P0-3 (TD-3)** Fixed `mapping/file_mapping.hpp`: removed the self-`#include`; **moved the OS-backend `#include`s to global scope** (they were nested inside `namespace peelf`, which defined the backends as `peelf::peelf::*` and left `NativeFileMappingBackend` bound to an incomplete type — only masked previously by the redundant top-level include); corrected the `// namespace ws::fs` comments to `peelf` across all mapping headers/sources; and removed the unused `win32_ec`/`posix_ec` static helpers (cleared MSVC C4505).
- [x] **P0-4 (TD-2)** `FileMapping`: the throwing constructor now reports failure via `std::system_error` (no more discarded `error_code`), with `open()` retained as the non-throwing/`error_code` path. **Also fixed a latent move bug** found while writing tests: move-assignment transferred `backend_`/`byte_size_` but not `data_`, so a moved-to mapping reported `is_open()==false` and ownership was split incorrectly. Covered by `tests/file_mapping_test.cpp` (4 tests: read-only map, ctor-throws, open()-error, move ownership).
- [x] **P0-5 (TD-11)** Renamed `apps/viewer/src/dissassembler/dissassembler.hpp` → `apps/viewer/src/disasm/disassembler.hpp` (via the connector's `move_file`) and fixed the four `#include`s (`application.h`, `application.cpp`, `ui/ui_app.hpp`, `ui/ui_panels.hpp`) plus the `apps/viewer/CMakeLists.txt` source entry. Type names (`Disassembler`, `Instruction`, `Architecture`) were already spelled correctly. Pure rename, no behavior change — gated by the green suite + viewer build. The now-empty `src/dissassembler/` folder can be removed with `rmdir` (git won't track it once committed). Disassembler unit tests come with Phase 4 (P4-x) when it's wired into the model and linked against capstone.
- [x] **P0-6 (TD-5)** Removed the dead `gui/gui.cpp` + `gui/gui.h` path (duplicate `open_file_dialog`/`load_file` free functions and the unused `menubar` struct) from the `peelf_viewer` build, and dropped the stale `#include "gui/gui.h"` from `application.cpp`. The live flow is unchanged (`Application::open_file_dialog` → `BinaryModel::load_file`; menu via `UiApp::render_main_menu`). Also clears the C4189 unused-local warnings. _Manual step (no delete tool available to the assistant):_ `git rm apps/viewer/src/gui/gui.cpp apps/viewer/src/gui/gui.h`. (Consolidating the two PE parsers — `peelf::parse_pe_bytes` vs `viewer::PeParser` — is tracked under P1-3.)
- [x] **P0-7 (TD-9)** `Application::open_file_dialog` now frees the NFD path with `NFD_FreePath` (was `free`), and `NFD_Init`/`NFD_Quit` are paired once at startup/shutdown (guarded by a `nfd_initialized_` flag so `NFD_Quit` only runs after a successful init) instead of `NFD_Init` on every open. Added `NFD_ERROR` handling via the logger. _No isolated unit test:_ this is native-dialog resource management with no pure-logic seam; gated by the green suite + a manual check that Open works and shutdown is clean. A testable `IFileDialog` seam can be added later if desired.
- [x] **P0-8 (TD-4)** Removed the unused, broken `IByteReader`/`byte_reader`/`make_reader` trio from `peelf/peelf.hpp` (non-virtual "interface" with undefined base methods — `make_reader` could never dispatch; also relied on `<bit>`/`<cstring>`/`<memory>` transitively). Left a note pointing to P1-4 where a proper endian reader returns with the unified model. Added `tests/peelf_core_test.cpp` covering `peelf::to_string(FileKind)` — the core's first direct unit test.
- [x] **P0-9** Added `libs/peelf_core/include/peelf/error.hpp` with `Error`, `Result<T> = std::expected<T, Error>`, and a `make_error("...")` helper that captures the call-site `source_location` (the robust defaulted-parameter idiom). Moved `Error` out of `peelf.hpp` (which now includes `error.hpp`); **declared** `parse_pe_bytes`/`parse_elf_bytes` publicly returning `Result<FileInfo>` (they were defined-but-undeclared); migrated both `.cpp` parsers from `std::unexpected(Error{...})` to `make_error(...)`. Added `tests/parsers_test.cpp` (4 tests): ELF success path against `hello.elf` (class/data/type/machine asserts) plus ELF and PE error paths. Suite now 12 tests. (Public declaration also unblocks the P1-3 parser consolidation.)
- [x] **P0-10** Added `.clang-tidy` (baseline: bugprone / performance / portability / modernize / misc / clang-analyzer / readability with the noisy checks disabled, `HeaderFilterRegex` scoped to `libs/peelf_core`, `apps/viewer/src`, `tests`; not yet gating). Wired into CMake: opt-in `PEELF_ENABLE_CLANG_TIDY` sets `CXX_CLANG_TIDY` **per target** (peelf_core / peelf_viewer / peelf_tests only — never the FetchContent deps; Ninja/clang presets only, since the VS generator ignores it), plus `format` / `format-check` custom targets driven by the existing `.clang-format`. CI enforcement is P7-3; the warning backlog is P7-4. Default builds are unchanged (option OFF; format targets are inert unless invoked).

**Exit criteria:** clean configure+build on MSVC and Clang with no warnings from our targets; no dead code paths; docs match build.

## 6. Phase 1 — Unified core parsing model

- [x] **P1-1** Added `libs/peelf_core/include/peelf/binary_image.hpp`: the `IBinaryImage` interface plus shared value types `Format`, `ImageKind`, `Architecture`, `Endianness`, and `Section`, each with a `to_string`. Concrete images are internal to `binary_image.cpp`; callers use `parse_image()`. (Symbols/imports/exports join the interface in Phase 3; `Section` is populated in Phase 2.)
- [x] **P1-2** `detect_format()` dispatches on `\x7fELF` / `MZ` (unknown magic rejected gracefully by `parse_image`). `ImageKind` is populated from the PE `IMAGE_FILE_DLL` characteristic and the ELF `e_type` (`ET_EXEC`→Executable, `ET_DYN`→SharedLibrary, `ET_REL`→Object, `ET_CORE`→Core). _Caveat:_ PIE executables are `ET_DYN` and currently report as SharedLibrary; `DT_FLAGS_1`/`DF_1_PIE` disambiguation is deferred to Phase 3 (dynamic info).
- [~] **P1-3** **PE parser (core done):** `PeImage` (internal to `binary_image.cpp`) parses DOS → PE signature → COFF → optional header (PE32/PE32+) for identity: machine→`Architecture`, `IMAGE_FILE_DLL`→`ImageKind`, magic→64-bit, entry VA = ImageBase + AddressOfEntryPoint, all bounds-checked. Tested via synthetic PE32+ plus deterministic PE32 and PE32+ fixtures. **Remaining:** data directories + section table (Phase 2), and **removing the two legacy parsers** (`peelf::parse_pe_bytes`, `viewer::PeParser`) once the app's `BinaryModel` is migrated onto `IBinaryImage` (P1-6) — deferred because that step is GUI-coupled and not build-verifiable from here. (TD-5)
- [~] **P1-4** **ELF parser (core done):** `ElfImage` parses the ELF identity for both ELF32/ELF64 and **little- and big-endian** — the endian-aware readers in `binary_image.cpp` restore the BE support the old `parse_elf_bytes` rejected: class→64-bit, data→endianness, `e_type`→`ImageKind`, `e_machine`+class→`Architecture` (including RISC-V32 vs RISC-V64), `e_entry`→entry point, bounds-checked. The full ELF file header (`e_ident`, type, machine, version, entry, table offsets/counts/sizes, flags, and section-name index) is exposed via `IBinaryImage::elf_header()` and tested across the deterministic endian/class/architecture matrix. Reachable via the public `parse_image()` and now **wired into the app** (`BinaryModel::load_file`, see P1-6) so ELF files load. **Remaining:** extended numbering resolution (`SHN_XINDEX`, `PN_XNUM`) and richer validation as section/program-header parsing is completed. (TD-6)
- [~] **P1-5** Bounds-safety pass: every header/table read goes through checked accessors; fuzz the parsers against truncated/garbage input (ties into existing `fuzzing-experiments` interest). **Progress:** unified PE/ELF parser section-table and optional-header arithmetic now uses subtractive/checked range logic; PE optional-header magic is validated; tests cover unsupported PE optional magic and an overflowing ELF section table. Remaining: convert all parser reads to a shared checked `ByteReader`/cursor and fuzz broadly.
- [~] **P1-6** First slice done (additive, low-risk): `BinaryModel::load_file` now calls `peelf::parse_image()` and stores a `std::unique_ptr<peelf::IBinaryImage>`, exposed via `BinaryModel::image()`. **ELF files now load in the app for the first time** — the File panel shows format/arch/kind/endianness/64-bit/entry from the unified image, and the Hex view works (bytes are read). PE still flows through the legacy `PeModel`/`PeParser`, so the PE-specific panels are untouched; every PE panel already guards on `pe()==null`, so ELF is safe (those panels just show "No PE file loaded"). **Remaining:** migrate the PE-specific panels onto `IBinaryImage`, then delete the legacy parsers (`viewer::PeParser` + core `parse_pe_bytes`/`parse_elf_bytes`); and factor the viewer model into a small library so `BinaryModel` is unit-testable (today it's app-only, gated by the core tests + the viewer build).

**Exit criteria:** load a PE exe, PE dll, ELF exe, and ELF .so and get correct headers + `ImageKind` + architecture for each.

## 7. Phase 2 — Sections, segments & raw bytes

- [~] **P2-1** Section **table** populated for both formats in `binary_image.cpp`, exposed via `IBinaryImage::sections()`: name (PE inline 8-char; ELF resolved via `.shstrtab`), virtual address (PE = ImageBase+RVA; ELF `sh_addr`), virtual size, file offset/size (ELF `SHT_NOBITS`→0), and R/W/X (PE `IMAGE_SCN_MEM_*`; ELF `SHF_ALLOC`/`WRITE`/`EXECINSTR`), all bounds-checked. ELF program headers are exposed as raw `ElfProgramHeader` values via `IBinaryImage::elf_program_headers()` and projected to shared `Segment` values. ELF section headers are exposed as raw `ElfSectionHeader` values via `IBinaryImage::elf_section_headers()` and projected to shared `Section` values. Tested against the deterministic ELF endian/class/architecture matrix and PE32+ x64 fixture. App: the Sections panel shows sections and ELF segments from `IBinaryImage`; section rows now navigate Hex to the section byte range, highlight the exact range even when it starts/stops mid-line, and refresh Disassembly for executable sections. **Remaining:** section↔segment mapping.
- [~] **P2-2** Sections panel migrated onto `IBinaryImage`: reads `model_.image()->sections()` / `segments()` (one source for PE and ELF) and shows **R/W/X permission flags** instead of a raw hex value, uniformly across formats. `IBinaryImage` also now owns checked file-offset ↔ virtual-address mapping over parsed sections, covered by PE/ELF fixture tests. Section click-through now updates the shared selection, Details, Hex, and executable-section Disassembly. **Remaining:** sortable columns and code/data chips. _Note:_ `BinaryModel::sections()` (the legacy `SectionInfo` vector) is now read by no panel — a cleanup candidate once the remaining panels migrate.
- [~] **P2-3** Raw-bytes/hex view: entry-point positioning on file load now resolves entry VA → file offset when mapped; cross-panel range navigation can scroll Hex to a target offset and highlight selected byte ranges; section/symbol/import/export/structure selections can follow into Hex when they expose a file offset. A pre-existing duplicate-ImGui-ID bug was also fixed: each byte cell's `Selectable` carries a unique `##<offset>` suffix in its label. **Remaining:** virtualized hex+ASCII for large files (use the existing `FileMapping` so we don't load everything into RAM), offset/RVA toggle, explicit go-to-offset/go-to-VA commands, range selection UX, and automated navigation/highlight tests. Hex must also have an explicit unavailable state: if the selected object has no file-backed/live-readable bytes, show a centered/watermark-style `(Hex Not Available)` message instead of leaving stale bytes/highlights visible.

**Exit criteria:** select a section in any loaded binary and land on its bytes in the hex view.

## 8. Phase 3 — Imports / exports / symbols

- [~] **P3-1** **PE imports:** parse the import directory and delay-load directory; resolve DLL names + named/ordinal functions; show the IAT. **Progress:** unified `PeImage` parses named PE imports into shared `ImportEntry` values with library, function name, IAT VA, and delay-load marker; deterministic fixtures cover normal `KERNEL32.dll!ExitProcess` and delay-load `USER32.dll!MessageBoxA`. Remaining: ordinal imports and malformed import-table negative tests.
- [~] **P3-2** **PE exports:** parse the export directory (names, ordinals, forwarders). **Progress:** unified `PeImage` parses named PE exports into shared `ExportEntry` values and the unified Exports panel displays them; deterministic PE32+ x64 fixture covers one named export, Docker-generated PE DLL fixtures now cover exported call-graph roots, and PE export symbols now recover bounded function sizes from x64 runtime-function ranges, exact COFF symbols, or nearest later symbols/exports in the executable section. Remaining: forwarder fixture/test coverage, malformed export-table negative tests, deeper COFF symbol-table exposure, and PDB/runtime metadata validation for optimized/stripped real-world DLLs.
- [~] **P3-3** **ELF dynamic info:** `DT_NEEDED` libraries, dynamic symbol table (`.dynsym`), and (optionally) `.symtab`; relocations enough to label PLT/GOT entries. **Progress:** `.symtab` and `.dynsym` symbols are parsed into shared `Symbol` values (name, address, size, binding, type, section index, table kind); raw ELF symbol entries are exposed via `IBinaryImage::elf_symbols()` with `st_name`, `st_info`, `st_other`, binding/type/visibility, `st_shndx`, `st_value`, `st_size`, and table kind; raw dynamic entries are exposed via `IBinaryImage::elf_dynamic_entries()` with `d_tag`, `d_val`/`d_ptr`, and resolved `DT_NEEDED` library when applicable; raw `SHT_REL` / `SHT_RELA` relocations are exposed via `IBinaryImage::elf_relocations()` with `r_offset`, `r_info`, decoded symbol index/type, and optional addend; raw SysV `.hash` and GNU `.gnu.hash` tables are exposed via `IBinaryImage::elf_sysv_hash_tables()` / `elf_gnu_hash_tables()`; and `DT_NEEDED` libraries are parsed into shared `ImportEntry` values. Covered by deterministic ELF32/ELF64 little-/big-endian fixture matrix across x86, ARM, RISC-V, MIPS, and PowerPC. Remaining: PLT/GOT labels, additional dynamic tags beyond raw exposure, and richer real-world fixture coverage.
- [~] **P3-4** Map all of the above onto shared `ImportEntry`/`ExportEntry`/`Symbol` types so the panels are format-agnostic, with a small "format-specific extras" expander. **Progress:** shared `Symbol` / `ImportEntry` / `ExportEntry` types, Symbols panel, unified Imports panel, and unified Exports panel exist. A viewer-side `SymbolIndex` now loads a unified symbol record set on file load, combining entry point, parsed symbols, exports, imports, and PE COFF function symbols with name/address lookup for graph/navigation use before graph-node clicking is enabled. Remaining: format-specific extras, demangling, richer PE COFF/PDB symbol metadata, and retiring legacy PE-specific panels after confidence builds.
- [~] **P3-5** **ELF full-spec metadata backlog:** relocations (`.rel*` / `.rela*`), interpreter path (`PT_INTERP`), notes (`PT_NOTE`, `.note.*`), and SysV/GNU hash tables now have parser/model/test coverage across the deterministic fixture matrix. Remaining: PLT/GOT mapping, additional dynamic tags beyond `DT_NEEDED`, symbol versioning (`.gnu.version*`), GNU properties, TLS, init/fini arrays, compressed sections, section groups, and OS/processor-specific flags. Keep each feature behind shared value types where possible and add targeted malformed-table tests.
- [~] **P3-6** **PE full-spec metadata backlog:** base relocations now parse into raw `PeBaseRelocationBlock` / `PeBaseRelocationEntry` values exposed via `IBinaryImage::pe_base_relocations()`, debug directories parse into raw `PeDebugDirectory` values with CodeView RSDS GUID/age/PDB path extraction, runtime-function entries parse from the exception directory into `PeRuntimeFunction`, bound imports parse into `PeBoundImport`, resource directories and bounded type/name/language data entries parse into `PeResourceDirectory` / `PeResourceDataEntry`, CLR/COM descriptors parse into `PeClrHeader`, TLS directories parse into raw `PeTlsDirectory` values with PE32/PE32+ fixed fields plus callback VA arrays, certificate tables parse into raw `PeCertificate` values using the PE-specific file-offset directory semantics, and Load Config directories parse into raw `PeLoadConfigDirectory` values including security cookie, SEH table/count, Guard CF pointers/table/count, and GuardFlags. These are covered by deterministic PE32/PE32+ fixtures. Structure navigation now exposes runtime functions, TLS, certificates, load config, bound imports, resource directory/data entries, and CLR header in addition to relocations/debug directories. Remaining: named resource strings and resource payload decoding (version/icon/manifest), richer CFG/security metadata beyond modeled Load Config fields, deeper unwind-info parsing (`.xdata`), section COMDAT/COFF symbol data, rich header (optional), overlay detection, ordinal-import coverage, forwarder-export coverage, and malformed-table tests. Add PE DLL fixture coverage as these land.
- [~] **P3-7** **Debug symbol lookup:** support external symbol/debug information association and lookup. **Progress:** PE CodeView PDB references now drive local sibling-path discovery, the viewer has **File → Open Debug Symbols...** for an explicit `.pdb` path, and Windows builds use the DIA SDK when present to load PDB function/public symbols into the shared `SymbolIndex`. The DIA backend first tries the registered COM class and then falls back to loading the discovered `msdia140.dll` directly, so Visual Studio installs work without requiring `regsvr32`; obvious non-PDB files are rejected before DIA. Debug symbols now participate in address/name lookup and the loaded-image call graph uses the enriched index so a PDB-provided `WinMain`/`main` replaces the unresolved placeholder. Covered by tests for missing PDB paths, invalid PDB rejection, DIA reachability against the build PDB, debug-symbol merging, and call-graph resolution from loaded symbols. Remaining: symbol-server configuration/cache policy, GUID+age validation against the PDB, surfacing loaded debug-symbol records in the Symbols panel, disassembly label substitution, COFF symbol-table parsing, and an LLVM/PDB fallback. ELF: DWARF discovery from `.debug_*`, build-id lookup, split debug files, `.gnu_debuglink`, and local debug package paths remain pending. Results should feed shared symbol/name lookup for panels, disassembly labels, and future decompilation.

**Exit criteria:** imports/exports populate for a PE dll and "needed libs + dynamic symbols" populate for an ELF .so.

## 9. Phase 4 — Disassembly

The Capstone wrapper (`disassembler.hpp`) is solid; wire it into the model and UI.

- [~] **P4-1** Disassembler architecture is now selected from `IBinaryImage::architecture()` (mapped to the Capstone wrapper's enum) on load, for **both** PE and ELF. Capstone is built with X86, ARM, ARM64, MIPS, PowerPC, and RISC-V support, and the viewer passes image endianness into the disassembler for endian-sensitive architectures. The Hex view's click-to-disassemble (`UiApp::disassemble_at_offset`) disassembles a 256-byte window of raw bytes at the clicked file offset and uses core file-offset → VA mapping when available. ELF entry-point disassembly now resolves entry VA → file offset through `IBinaryImage`; PE still uses the legacy `PeModel` path until the remaining PE panels migrate. Structure/section/symbol/export selections now refresh Disassembly when the selected VA/file offset maps to executable bytes; imports stay hex/details-focused because they usually select pointer/data slots. **Remaining:** auto-pick the executable section's bytes in all cases, clamp disassembly windows to mapped executable ranges, and add model-level disassembly/navigation tests.
- [ ] **P4-2** Disassembly panel: address / bytes / mnemonic / operands columns, syntax coloring (reuse the existing `is_branch/is_data_movement/is_simd/...` classifiers), and synchronized scrolling with the hex view.
- [~] **P4-3** Navigation: section, symbol, export, and structure selections now jump Hex to the selected bytes and refresh Disassembly where the target maps to code; import selections jump Hex/Details to the import address slot when available. **Remaining:** double-click/keyboard navigation, call/branch target resolution to known symbol names, import thunk/IAT disassembly decisions, and tests for invalid/unmapped selections.
- [~] **P4-4** Verify Capstone mode selection and add ARM/Thumb toggle hookup (TD-14). **Progress:** ARM64 initialization no longer uses the ARM32 mode flag; focused disassembler tests cover x86-64, ARM64, MIPS32/MIPS64, PowerPC32/PowerPC64, RISC-V32, and RISC-V64 NOP decoding. Remaining: complete ARM/Thumb UI hookup and model-level disassembly tests for each parsed fixture.
- [~] **P4-5 Call-flow / CFG graph export:** first graph-model slice added in `graph/call_graph.*`: architecture-neutral `std::uint64_t` address/range metadata, stable node ids, optional VA/RVA/file-offset fields, symbol refs, node/edge kinds, DOT emission, Graphviz SVG/PNG/BMP command construction, and an injectable process runner with `CreateProcessW` on Windows. `build_entry_call_graph()` now builds a first parser-backed graph from the currently loaded `IBinaryImage`: entry point, entry symbol, matching exports, imports, ELF `_start` → `__libc_start_main` → `main` startup hints when symbols are present, and unresolved PE CRT/`WinMain` placeholders when symbols are not available. Graphviz detection now normalizes the installed `dot.exe` path for compile definitions. Tests cover DOT content, SVG/PNG/BMP command construction, fake-runner rendering, real SVG rendering when `dot` is detected, loaded-image PE/ELF graph construction, and symbol lookup across x64 and RISC-V64 fixtures. **Remaining:** construct CFGs from real disassembly, classify branch/call edges per architecture, recover PE startup/CRT-to-`WinMain` flow from actual code/PDB when available, connect graph nodes to `ViewerSelection`, and decide the later interactive renderer.

**Exit criteria:** open a binary, jump to entry point, see correctly disassembled code with working branch navigation.

## 10. Phase 5 — Decompilation

- [ ] **P5a-1** Define `IDecompiler` abstraction (`decompile(function_or_range) -> Result<DecompiledUnit>`), independent of backend (D1).
- [ ] **P5a-2** In-tree **pseudocode-lite** first pass: function boundary detection, basic-block/CFG reconstruction over Capstone output, annotated/structured listing. Ships without external deps and validates the UI.
- [ ] **P5b-1** Integrate the chosen real backend (D1; default RetDec via subprocess): detect availability, run on the selected function/range, parse output into `DecompiledUnit`.
- [ ] **P5b-2** Decompile panel: C-like output with line→address mapping, synchronized highlight with the disassembly panel, graceful "backend not installed" state.
- [ ] **P5b-3** **Binary Ninja backend:** optional backend for users with Binary Ninja Free/personal/non-commercial/commercial installed. Start with a Python subprocess bridge (`bn_decompile.py`) that receives file path + target VA/range, opens the Binary Ninja `BinaryView`, waits for analysis, finds/creates the containing function where possible, exports HLIL/decompiled C-like text plus line/address metadata as JSON, and returns structured errors for missing install, license/API failure, unsupported architecture, analysis timeout, or no function found. Do not link against BN SDK in the first slice.
- [ ] **P5b-4** **Binary Ninja discovery/configuration:** detect BN from PATH, common install locations, user-configured path, and BN settings/last-run hints where available. Expose backend availability and version/capability status in the UI. Support architecture capability checks so Binary Ninja Free limitations (notably x86/x64-focused decompilation) are reported clearly instead of silently failing on ARM64/RISC-V/MIPS/PowerPC fixtures.
- [ ] **P5b-5** **Binary Ninja direct SDK spike:** after the Python bridge works, evaluate direct C++ SDK integration. Criteria: stable headers/libs on Windows and Linux, acceptable build isolation, no required redistributable BN binaries, clean optional CMake switch, and no CI dependency on BN. Keep Python bridge as fallback unless direct SDK is clearly better.

**Exit criteria:** select a function and see pseudo-C; with the backend installed, see real decompiled C synced to the disassembly.

## 11. Phase 6 — UI/UX & rendering hardening

- [ ] **P6-1 (TD-7)** Handle `VK_ERROR_OUT_OF_DATE_KHR` / `VK_SUBOPTIMAL_KHR` from `vkAcquireNextImageKHR` and `vkQueuePresentKHR` → trigger swapchain recreation instead of throwing/crashing on resize.
- [ ] **P6-2 (TD-13)** Move to proper frames-in-flight sync (per-frame semaphores/fences) rather than a single reused set.
- [ ] **P6-3 (TD-8)** Make font-texture upload explicit and version-correct in `init_imgui` rather than relying on a comment/lazy init.
- [~] **P6-4** **Default docking layout and reset:** first slice implemented with ImGui DockBuilder. On first run, the viewer builds a deterministic dock workspace: Structure/File/Sections/Symbols on the left, Hex in the primary center area, Disassembly below the center, Details plus Imports/Exports/PE-specific detail panes on the right, and Log at the bottom. **View → Reset Layout** rebuilds the default while panes remain dockable/tearable. Remaining: decide persisted-layout migration/versioning for users with older broken `imgui.ini` state, consolidate primary tabs from P6-12, and add recent-files/About dialog.
- [ ] **P6-5** File-open robustness: surface parse errors to the log panel (the `Logger` already exists) instead of failing silently.
- [x] **P6-6 (TD-16)** Collapsed the duplicate **Disassembly** window: `UiApp` now renders only the richer table view in `render_disassembly_panel()`, the duplicate `DisassemblyPanel::draw()` path is no longer owned/rendered by `UiApp`, and the dead `disasm_panel_.current_instructions_ = current_instructions_` self-assignment was removed. The panel can be shown/hidden from the View menu.
- [ ] **P6-7** **Live process viewing foundation:** enumerate running processes, choose a target, and open a read-only live image view. Windows first target: local x64 processes, process id/name/path/session/user/bitness/integrity where available, module list, thread count, handle count where available, parent process, command line, start time, image path, and graceful access-denied reporting. Linux first target: local x64 processes through `/proc`, pid/name/exe/cwd/cmdline/status/maps, UID/GID, threads, and graceful permission failures. The UI must distinguish file-backed images from live-memory views.
- [ ] **P6-8** **Live image parsing model:** adapt `IBinaryImage` concepts to loaded modules where file offsets may not exist, sections are mapped at runtime addresses, relocations are applied, and pages can be missing/unreadable. Add process-module navigation, live memory hex view, and disassembly over mapped ranges. Keep this separate from static-file parsing so tests can use a fake memory provider.
- [~] **P6-9** **XPEViewer-inspired structure navigator:** first model slice added in `navigation/viewer_navigation.*`: `build_structure_tree()` creates a stable hierarchy from `IBinaryImage` for File/Image → headers → sections → segments → imports/exports/symbols → PE relocations/debug directories → ELF interpreter/dynamic entries/relocations/notes/hash tables. Fixture-backed tests cover PE and ELF tree contents. The left-side `Structure` panel renders the tree, updates the current selection on single-click, fills Details, and now drives Hex/Disassembly navigation for mappable selections while de-duping repeated frame updates. Remaining: double-click/Enter navigation, context menus, richer node labels/icons, tree filtering, and behavior tests beyond tree-content tests.
- [~] **P6-10** **Selection and navigation model:** first ImGui-independent selection payload added via `ViewerSelection`: kind, display label, optional file offset, optional VA, size, backing object index, and preferred view (`details`, `hex`, `disassembly`). Tests verify executable sections prefer disassembly and parser objects expose expected locations. `UiApp` now owns a current selection, rebuilds the structure tree on file load, and panel/structure selections are consumed by Details plus Hex/Disassembly navigation paths instead of remaining display-only. Remaining: extract the navigation application logic into a testable viewer-model layer, add offset/RVA/VA command support, and cover failure cases for unmapped/virtual-only selections. Selection/focus changes need explicit navigation intent so table focus, tree focus, Details focus, and keyboard traversal do not cascade accidental Hex/Disassembly changes; only deliberate selection activation should mutate the primary byte/disassembly views unless a future linked-focus mode is enabled.
- [~] **P6-11** **Details and inspector panes:** first `Details` pane added on the right. Single-clicking a structure node fills it with kind, label, preferred view, file offset, VA, size, and backing object index; clicking sections, symbols, imports, and exports now also updates the shared selection so Details tracks table-driven navigation. Remaining: right-click context menu actions ("Show details", "Go to offset", "Go to RVA/VA", "Open in hex", "Disassemble here", "Copy value", "Copy row as JSON") and richer parser-specific property tables.
- [ ] **P6-12** **Primary work area tabs:** make the central dock a small set of predictable tabs: Summary/Details, Hex, Disassembly, Decompile, and Log/Diagnostics as needed. Avoid many free-floating one-off windows for parser structures; prefer one tree selection plus a reusable details table. Panels may still be torn off through ImGui docking.
- [~] **P6-12a** **Call graph / CFG panel:** first floating ImGui Call Graph window added behind **View → Call Graph**. It now defaults to a loaded-image graph built from the active `IBinaryImage`, renders it through Graphviz to BMP, uploads it as a Vulkan texture, draws it on a white canvas, and scales it to fit the panel. Manual PE x64 and Linux ELF sample graph buttons remain as fallbacks/demos. Loading a new file invalidates and rebuilds the panel graph when visible, and `UiApp` now builds/logs a loaded `SymbolIndex` before future graph-node clicks are allowed. The graph builder can now consume the enriched viewer `SymbolIndex`, including PDB-loaded symbols, so PE startup graphs can replace the unresolved `WinMain` placeholder when the matching PDB is loaded. First graph-click path is implemented: loaded-image graphs also render Graphviz `plain` layout metadata; clicking a node hit-tests back to the graph node, updates `ViewerSelection`, moves Hex/Disassembly to the node's bytes when available, and loads a first-pass symbol fan-out graph rooted at the clicked function. **Remaining:** replace parser-backed entry/import/symbol sketches with real disassembly CFGs, improve fan-out target resolution beyond direct calls, dock it into the central tab set, add zoom/pan, preserve crisp SVG/vector rendering or higher-resolution raster output, add richer hit-testing/hover feedback, and wire graph node activation into the broader selection/view-model tests.
- [ ] **P6-12b** **Graph interaction renderer decision:** after DOT export and static rendering work, evaluate the interactive path with real selection requirements: WebView2 + Mermaid/SVG/HTML, rendered SVG with hit-test metadata, or native ImGui drawing. Decision criteria: clickable nodes/edges, address metadata, zoom/pan quality, theming, offline behavior, deployment complexity, and whether graph clicks can update the same `ViewerSelection` model as tree/table clicks.
- [ ] **P6-13** **UI visual and density pass:** move toward a quiet reverse-engineering-tool layout: dense tables, stable columns, sortable/filterable rows, clear address/offset formatting, small icons for node kinds/actions where useful, no oversized cards/marketing layout, and no explanatory text inside the app unless it is an empty/error state.
- [ ] **P6-14** **Process inventory panel:** Process Explorer / Process Hacker inspired table with sortable/filterable rows: PID, process name, image path, architecture, user, session, integrity/elevation where available, start time, CPU/memory snapshots where feasible, parent PID/name, thread count, handle count, loaded module count, and access status. Opening a row creates a read-only live target.
- [ ] **P6-15** **Live module panel:** show mapped executable modules for a live process: module name/path, base address, image size, entry point, architecture, file-backed path, load order where available, PE/ELF parse status, version/company metadata where available, and security-summary badges. Selecting a module updates Structure/Details/Hex/Disassembly against that module's live address space.
- [ ] **P6-16** **VMMap-style memory map:** show committed/reserved/free or readable mappings as the platform permits. Windows: `VirtualQueryEx` regions with base, size, state, type, protection, allocation base/protection, backing file via mapped-file APIs where available. Linux: `/proc/<pid>/maps` regions with address range, permissions, offset, device/inode, pathname, anonymous/heap/stack/vdso labels. Include grouping by module/file, private/shared/image/heap/stack categories, protection filters, and unreadable-region handling.
- [ ] **P6-17** **Live memory byte provider:** introduce `IByteProvider` / `IMemoryProvider` abstraction for static file bytes, module bytes, and process memory. Reads must report partial reads and protection failures without crashing. Windows provider uses `ReadProcessMemory`; Linux provider uses `process_vm_readv` or `/proc/<pid>/mem` where permitted. Hex/disassembly must consume this abstraction before live-memory UI is enabled.
- [ ] **P6-18** **Threads, handles, and descriptors:** harvest useful read-only process metadata after the first memory/module slice. Windows: threads with start address/TID/state if available, handles grouped by type/name where permissions allow, token/user/integrity, environment block if safely readable. Linux: threads from `/proc/<pid>/task`, file descriptors from `/proc/<pid>/fd`, namespaces/cgroups/status fields. Keep expensive or privileged fields lazy-loaded.
- [ ] **P6-19** **Security hardening report panel:** BinSkim-style facts for static files and live modules. PE: ASLR/DYNAMIC_BASE, NX/DEP, CFG/GUARD_CF, SAFESEH where applicable, high-entropy VA, relocation presence, load config, Authenticode/certificate table, debug/PDB info, timestamp/reproducible build clues, section W+X or executable writable data, imports of risky APIs, delay-load/bound imports, CLR metadata. ELF: PIE, NX via GNU_STACK, RELRO, BIND_NOW, stack canary indicators, RPATH/RUNPATH, interpreter, needed libraries, text relocations, W+X segments, stripped/debug info/build-id, symbol visibility, RELA/PLT/GOT facts. Output should include severity, rule id, evidence, and remediation text.
- [ ] **P6-20** **Live target navigation:** extend the Structure tree with Processes → Modules → Memory Regions → Threads/Handles/FDs → Security Findings. Single-click fills Details; double-click navigates Hex/Disassembly to the best live VA. Stale selections must be detected if a process exits, a module unloads, or a memory region changes.
- [ ] **P6-20a** **Library/module dependency tree:** add a shared dependency tree view that works in both static and dynamic analysis. Static mode: PE imports/delay-load/bound imports and ELF `DT_NEEDED`/interpreter/RPATH/RUNPATH become library nodes with imported symbols beneath them, unresolved status, and known search-path hints. Dynamic mode: live process modules become loaded library nodes with base address, path, load order, reference edges where available, and resolved imported symbols when parser/runtime data allows. The same selection model should drive Details/Hex/Disassembly for both modes, with missing/unloaded/unresolved dependencies clearly marked.
- [~] **P6-20b** **Paged editable image foundation:** implement `PagedBinaryImage` plus normalized patch overlay intervals in `peelf_core`. Original file bytes remain immutable and are paged from disk; effective reads merge file pages with patched intervals. Transactions record original/effective bytes for undo/redo and dirty-range highlighting. Progress: first core API and tests cover page-boundary reads/writes, overlapping patches, undo/redo, and reverted-byte cleanup on MSVC and Docker/Clang. Remaining: save/export API, temp-workspace backing for large generated pages, patch metadata timestamps/notes, and integration into `BinaryModel`.
- [ ] **P6-21** **Hex/patch editor mode:** add a pencil icon and keyboard shortcut to enter explicit edit mode from Hex, Structure, or Disassembly selections. Highlight selected byte ranges in the Hex view, support range selection, show original vs patched bytes, validate fixed-width hex entry, and keep edits staged through the `PagedBinaryImage` transaction API until Apply/Discard. Dirty ranges should be visually distinct and listed in a patch set panel.
- [ ] **P6-22** **Patch editor dialog:** opening edit mode shows a split patch window: top half is original bytes plus current disassembly for the selected range; bottom half is editable replacement input. Support two input modes: raw hex bytes and assembly text. Show decoded replacement disassembly before applying, byte-count delta warnings, file-offset/RVA/VA context, and whether a patch changes executable code, data, headers, or unmapped bytes. Applying from this dialog must create one transaction so undo restores the full edit atomically.
- [ ] **P6-23** **Assembler provider architecture:** add an optional `IAssemblerProvider`/`AssemblerProvider` abstraction for assembly→bytes generation. Requests include architecture, bit width/mode, endianness, syntax, base address, and source text; results return bytes plus diagnostics, or an explicit unavailable/unsupported status. The UI must display unsupported architecture/provider cases as a modal or inline message such as "Assembler is not available for architecture foo" instead of failing silently. Keep direct byte editing available even when no assembler provider is available. Progress: the Shellcode scratch panel now accepts pasted hex/shellcode text and normalizes it to bytes; assembly mode is present but reports the missing backend until a provider is wired in.
- [ ] **P6-23a** **LLVM MC assembler provider:** add an LLVM-backed provider for broad cross-architecture assembly. First implementation should prefer external tools (`llvm-mc` with target triples plus `llvm-objcopy` extracting `.text`) so the viewer does not immediately link embedded LLVM. Later evaluate embedding LLVM MC APIs (`MCAsmParser`/`MCStreamer`) if diagnostics, relocations, or performance justify the dependency. Reject or explicitly report relocations/external symbols that cannot be represented as a fixed byte patch.
- [~] **P6-23b** **AsmTK/AsmJit assembler provider:** add a lightweight provider for the architectures AsmTK can parse, starting with x86/x64 if the dependency builds cleanly. Treat this as a fast/local backend, not the architecture-complete answer. Unsupported architectures should return an unavailable/unsupported result that the Shellcode/Patch UI can present cleanly. Progress: CMake now fetches pinned AsmJit/AsmTK sources behind `PEELF_ENABLE_ASMTK=ON`, exposes `peelf::asmtk`, and has a dependency smoke test that assembles x64 `nop; ret`; the actual provider wrapper is still pending.
- [ ] **P6-24** **Patch persistence/export:** support saving patched static files to a new path, exporting patch records as JSON, and later producing common patch formats if useful. Never overwrite the input file by default. Patch records should include original bytes, replacement bytes, file offset, VA/RVA when mapped, selection label, timestamp, and optional user note.
- [ ] **P6-25** **Lua scripting over patch API:** embed Lua as the first binary-modification scripting provider. Expose transaction-scoped `read_bytes`, `write_bytes`, typed endian reads/writes, `find_bytes`, and later `assemble`/`disassemble` helpers. Scripts must commit or rollback explicitly; failures rollback pending writes and report diagnostics in the Log panel. Python remains a later optional provider after the host API is stable.

**Exit criteria:** resize/minimize without crashes; consistent multi-panel layout; errors visible to the user; a loaded binary opens into a predictable tree/detail/hex/disassembly workspace, and any moved/torn layout can be reset.

## 12. Phase 7 — Quality, testing & CI

- [x] **P7-1** GoogleTest wired in via FetchContent under `tests/` (option `PEELF_BUILD_TESTS`, discovered by CTest) with a hello-world smoke test and an ELF-magic fixture test against `tests/fixtures/hello.elf`. _Next:_ dedicated PE/ELF parser tests using small fixture binaries + truncated/corrupt inputs (every new parser feature adds tests here).
- [~] **P7-2** Golden-file tests comparing parsed summaries against known-good output for a handful of real exe/dll/so/elf samples. **Progress:** added deterministic non-executable parser fixtures for ELF64 x86-64, ELF64 ARM64, ELF64 RISC-V64, ELF32 little-endian x86, ELF32/ELF64 big-endian MIPS, ELF32/ELF64 big-endian PowerPC, ARM32 little-/big-endian, ARM64 big-endian, RISC-V32 little-/big-endian, RISC-V64 big-endian, PE32 x86, and PE32+ x86-64, plus tests for architecture, kind, endianness, entry point, `.text` permissions, and core address mapping. Remaining: real-world exe/dll/so samples and richer golden summaries.
- [ ] **P7-3** CI (GitHub Actions): build matrix {MSVC, Clang18+Ninja} × {Debug, Release}, run tests, run `clang-format --dry-run` and `clang-tidy`. Warnings-as-errors for our targets (C3).
- [ ] **P7-4 (TD-12)** Warning cleanup pass: resolve `-Wconversion`/`-Wsign-conversion` hits; replace C-style casts (e.g. in `vulkan_manager.cpp`) with named casts.
- [ ] **P7-5** Address-/UB-sanitizer build target for the parsers; run the fuzzers from `fuzzing-experiments` against them in CI (nightly).
- [~] **P7-6** Docs: keep `README.md` build section current; add a short `ARCHITECTURE.md` describing `IBinaryImage` and the panel/viewmodel split. Progress: README now has current MSVC/Clang/Docker build steps, run/test commands, and current feature/dependency summary; `docs/ARCHITECTURE.md` adds Mermaid diagrams for runtime shape, static load flow, panel navigation, call graph rendering, assembler providers, dependency shape, and the fixture matrix.
- [ ] **P7-7 Parser robustness matrix** — grouped parser tests for malformed but plausible tables, not random bytes. Covers: PE import/export truncation and invalid RVAs; PE ordinal imports; PE forwarder exports; PE delay-load imports; ELF bad `sh_link`, bad `sh_entsize`, truncated `.dynamic`, invalid string offsets; ELF `.dynsym` distinct from `.symtab`; ELF relocations/PLT/GOT once implemented. These should be table-driven and fixture-backed where possible.
- [~] **P7-8 Cross-format fixture matrix** — known, documented, non-executable fixtures for PE exe, PE dll, ELF executable, and ELF `.so` across supported architectures. Minimum target: Win x64 exe+dll; Linux x64 exe+so; Linux ARM64 exe+so; Linux RISC-V64 exe+so. **Progress:** deterministic coverage exists for PE32/PE32+, ELF32/ELF64, ARM32/ARM64, RISC-V32/RISC-V64, MIPS32/MIPS64, PowerPC32/PowerPC64, little-/big-endian ELF parser identity, `.text` mapping, and Docker-generated `bin-matrix/` ELF executable plus `.so` fixtures with detached debug sidecars for x86, x86-64, ARM32, ARM64, RISC-V64, MIPS32/64 BE, and PowerPC32/64 BE. Docker now also generates non-executable MinGW PE DLL fixtures plus `.dll.debug` sidecars for x86 and x86-64, with tests for PE shared-library identity, imports, exports, and exported-function fan-out. Remaining: PE ARM64 DLL fixtures when a suitable Windows ARM64 cross toolchain is available, richer golden assertions for imports/exports/symbols on every supported arch, and real-world non-random samples where licensing permits.
- [x] **P7-8a Auto bin-matrix generation in CMake/CTest** — added `bin-matrix-docker-image`, `generate-bin-matrix`, and `test-bin-matrix` CMake targets. Added the opt-in `PEELF_ENABLE_BIN_MATRIX_DOCKER_TEST` CTest registration, off by default because it requires Docker and rewrites `bin-matrix/`. Documented the workflow in `README.md`.
- [ ] **P7-9 Disassembly behavior tests** — model-level tests for entry-point disassembly and mapped-address disassembly, not only Capstone wrapper tests. Covers: PE entry point, ELF entry point, x86-32 mode, ARM32 mode, ARM Thumb toggle, ARM64 fixture disassembly, MIPS/PowerPC/RISC-V fixture disassembly, file-offset→VA mapping during hex-click disassembly, and range limiting so disassembly never reads beyond section/file bounds.
- [ ] **P7-10 Navigation behavior tests** — navigation is now partially implemented in the UI, so the next step is to test it in a viewer-model layer independent of ImGui/Vulkan. Covers: startup entry-point positioning, section→hex/disassembly, segment→hex, import/export/symbol→hex or disassembly, structure-tree selection→Details/Hex/Disassembly, symbol/function selection→call graph, byte/disassembly selection→containing call graph/basic block, graph node selection→Symbols/Hex/Disassembly/Details, focus changes that must *not* mutate Hex/Disassembly/Call Graph, offset/RVA/VA display-mode switching, go-to-offset, go-to-VA, selection state persistence, range-highlighting boundaries, `(Hex Not Available)` for selections with no readable byte range, and failure cases for virtual-only bytes, invalid RVAs, and unmapped VAs.
- [~] **P7-11 Debug symbol tests** — deterministic fixtures for PDB/CodeView and DWARF lookup flows. **Progress:** tests cover CodeView missing-PDB behavior, explicit missing PDB load status, merging loaded debug symbols into the shared symbol index, and PE call-graph resolution using debug-loaded `WinMain`. Remaining: committed real/synthetic PDB fixture with GUID+age validation, local-path hit, DIA public/function enumeration golden assertions, symbol-server-disabled behavior, DWARF split-debug/build-id lookup, and merged symbol-name resolution in disassembly labels.
- [ ] **P7-12 Live process tests** — isolate live-process logic behind fake process/memory providers so CI can test process/module enumeration, region permissions, partial reads, stale mappings, module unload behavior, and live-memory VA navigation without requiring privileged OS process access.
- [ ] **P7-12a Dependency tree tests** — fake/static fixtures and fake/live process providers for library dependency graphs. Covers PE import libraries, ELF `DT_NEEDED`, unresolved libraries, duplicate imports collapsed under one library node, delay-load/bound import annotations once parsed, live loaded-module edges, module unload/stale states, and selection payloads for library and imported-symbol nodes.
- [~] **P7-12b Call graph / DOT renderer tests** — model tests for function/basic-block graph construction, edge classification, stable node ids, address/range metadata, and selection payload round-trips. **Progress:** tests cover DOT emission, Graphviz command construction for SVG/PNG/BMP/plain, fake-runner rendering, optional real Graphviz SVG rendering, PE/ELF loaded-entry sketches, PDB-backed PE user-entry resolution, substring false-positive prevention for `main`, Graphviz plain-layout parsing, first-pass symbol fan-out fallback when no direct calls resolve, known-call fan-out from Docker-generated ELF executables, ELF `.so` files, and PE DLL exports, plus bounded PE DLL export fan-out so x64 `shared_anchor` no longer scans through adjacent CRT helper code. Remaining: DOT golden/snapshot tests, true function/basic-block CFG construction, edge classification from disassembly details, UI-independent click→selection round-trip tests, and richer direct/indirect call target fixtures.
- [ ] **P7-13 Docking/layout tests** — factor default docking/layout decisions into a small testable layout-spec builder where possible. Tests should cover default pane identities/roles, reset-layout behavior, persisted-layout opt-out for first run, and required unique ImGui window IDs so panels do not stack or collide.
- [ ] **P7-14 Process inventory tests** — fake Windows and Linux process providers covering process lists, access-denied entries, exited processes, parent/child relationships, command-line parsing, `/proc` missing-field behavior, and sorting/filtering view models.
- [ ] **P7-15 Memory map tests** — fake VMMap-style region providers covering adjacent-region coalescing, protection labels, file-backed vs anonymous regions, heap/stack/module classification, partial reads, unreadable regions, process-exit invalidation, and live VA→region lookup.
- [ ] **P7-16 Security hardening tests** — fixture-backed rule tests for BinSkim-style reports. PE coverage: ASLR/NX/CFG/high-entropy VA/relocations/load-config/certificate/debug/W+X sections. ELF coverage: PIE/NX/GNU_STACK/RELRO/BIND_NOW/RPATH/RUNPATH/build-id/stripped/W+X segments/text relocations. Each test asserts rule id, severity, evidence string, and affected address/table where applicable.
- [ ] **P7-17 Decompiler backend contract tests** — fake backends for unavailable, timeout, unsupported architecture, no function, malformed JSON, and successful decompile with line→address mapping. These tests gate `IDecompiler`/UI behavior without RetDec/Ghidra/Binary Ninja installed.
- [ ] **P7-18 Binary Ninja optional integration tests** — opt-in tests enabled only when `PEELF_ENABLE_BINARY_NINJA_TESTS=ON` and BN is detected. Cover x64 PE and x64 ELF fixtures, backend discovery, analysis timeout handling, unsupported architecture reporting, and JSON schema stability from `bn_decompile.py`. Never require BN in default CI.
- [ ] **P7-19 Patch editor tests** — viewer-model tests for byte selection, highlighted ranges, staged edits, invalid hex input, apply/discard behavior, overlapping patches, read-only/unmapped ranges, original-vs-patched byte preservation, and save-to-new-file output. UI tests can stay minimal; patch semantics must be ImGui-independent.
- [ ] **P7-20 Assembler backend tests** — fake assembler backend tests for success, syntax error, unsupported architecture/mode, byte-count mismatch, and diagnostics. Optional Keystone integration tests should be gated behind `PEELF_ENABLE_KEYSTONE_TESTS=ON`; default CI must not require Keystone.

**Exit criteria:** green CI on both toolchains with tests, lint, and warnings-as-errors.

## 13. Tech Debt Register

Each item is owned by a Phase-0/6/7 task above.

| ID | Issue | Location | Fix task |
| --- | --- | --- | --- |
| TD-1 | Docs claim vendored deps + bootstrap scripts; build actually uses FetchContent | `README.md` (fixed), `third_party/README.md`, `scripts/*` | P0-1 |
| TD-2 | `FileMapping` ctor discards `open()`'s `error_code`; silent failure + unused-var warning | `mapping/file_mapping.hpp` | P0-4 |
| TD-3 | Header self-include + duplicate win32 include; wrong namespace comments | `mapping/file_mapping.hpp`, `file_mapping_win32.hpp` | P0-3 |
| TD-4 | `IByteReader` methods non-virtual → no polymorphic dispatch from `make_reader` | `peelf/peelf.hpp` | P0-8 |
| TD-5 | Two PE parsers + dead `gui.cpp` load path + unused `menubar` | `gui/gui.cpp`, core vs `model/pe_parser` | P0-6, P1-3 |
| TD-6 | ELF path unwired; `parse_elf_bytes` undeclared; `parse_file` commented out | `elf/elf_parser.cpp`, `peelf/peelf.hpp` | P1-4 |
| TD-7 | Swapchain out-of-date/suboptimal not handled; `check()` throws on resize | `vulkan/vulkan_manager.cpp` | P6-1 |
| TD-8 | ImGui fonts never explicitly uploaded (comment only) | `application.cpp` `init_imgui` | P6-3 |
| TD-9 | NFD path freed with `free()` instead of `NFD_FreePath` | `application.cpp` | P0-7 |
| TD-10 | ImGui pinned to moving `docking` branch tip → non-reproducible | `third_party/dependencies.cmake` | P0-2 |
| TD-11 | "dissassembler" misspelling (dir/file/usage) | `apps/viewer/src/dissassembler/` | P0-5 |
| TD-12 | `-Wconversion`/`-Wsign-conversion` hits; C-style casts | multiple, esp. `vulkan_manager.cpp` | P7-4 |
| TD-13 | Single-frame-in-flight; reused semaphores | `vulkan_manager.cpp` | P6-2 |
| TD-14 | Capstone ARM64 mode/option review | `disassembler.hpp` | P4-4 |
| TD-15 | No tests, CI, or lint enforcement | repo-wide | P7-1..P7-3 |
| TD-16 | Duplicate `Disassembly` ImGui window (`DisassemblyPanel::draw` + `render_disassembly_panel` both `Begin("Disassembly")`); plus a dead reference self-assign | `ui/ui_app.cpp`, `ui/ui_panels_disasm.cpp` | P6-6 |

## 14. Suggested sequencing

1. **Phase 0** (debt + foundations) — unblocks all.
2. **Phase 1** (unified model) — the spine everything hangs off.
3. **Phases 2 → 3 → 4** (sections/bytes → imports/exports → disasm) — incremental, each demo-able.
4. **Phase 6** rendering hardening can run in parallel once Phase 0 lands.
5. **Phase 5** (decompile) after Phase 4 and the D1 decision.
6. **Phase 7** (tests/CI) starts during Phase 1 (parsers are the highest-value test target) and continues throughout.

## 15. Backlog / stretch

- [ ] Mach-O support (third format) behind the same `IBinaryImage`.
- [ ] String extraction view; entropy/packer heuristics.
- [ ] Symbol demangling (Itanium + MSVC).
- [ ] PDB / DWARF debug-info association for better names in disasm/decompile.
- [ ] Search across sections (byte patterns, strings, regex).
- [ ] Export views to JSON/CSV.
- [ ] Diff two binaries.
- [ ] Patch formats: IPS/BPS-style patch export, JSON patch import/reapply, and binary diff-to-patch generation.
- [ ] Process Explorer / Process Hacker feature harvest: module/thread/handle/token/environment/network-style metadata as read-only process context, prioritized by safety and portability.
- [ ] Sysinternals VMMap feature harvest: memory classification, protection visualization, backing file grouping, private/shared/image/heap/stack summaries, and region diff snapshots.
- [ ] BinSkim feature harvest: formalize security rule IDs, evidence formatting, remediation text, baseline suppression, and JSON/SARIF export once the report panel is stable.
